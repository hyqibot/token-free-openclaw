import { type RefObject } from "react";
import { BubbleDataType } from "../BubbleList";
export interface UsePaginationItemsOptions {
    enable: boolean;
    order?: 'asc' | 'desc';
}
export declare const usePaginationItems: (items: (BubbleDataType & {
    history?: boolean;
})[], options: UsePaginationItemsOptions) => {
    items: (import("../..").BubbleProps<string> & {
        key?: string | number;
        role?: string;
        id?: string;
    } & {
        history?: boolean;
    })[];
    noMore: boolean;
    loadMore: (_scrollRef?: RefObject<HTMLElement | null>) => Promise<void>;
};
