function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
var _excluded = ["trigger"];
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(obj, key, value) { key = _toPropertyKey(key); if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : String(i); }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArray(iter) { if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter); }
function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) return _arrayLikeToArray(arr); }
function _objectWithoutProperties(source, excluded) { if (source == null) return {}; var target = _objectWithoutPropertiesLoose(source, excluded); var key, i; if (Object.getOwnPropertySymbols) { var sourceSymbolKeys = Object.getOwnPropertySymbols(source); for (i = 0; i < sourceSymbolKeys.length; i++) { key = sourceSymbolKeys[i]; if (excluded.indexOf(key) >= 0) continue; if (!Object.prototype.propertyIsEnumerable.call(source, key)) continue; target[key] = source[key]; } } return target; }
function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }
function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }
function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i]; return arr2; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }
import { Upload } from 'antd';
import { IconButton } from "@agentscope-ai/design";
import { SparkAttachmentLine } from "@agentscope-ai/icons";
import { Sender, Attachments } from "../../../..";
import React, { useCallback, useRef, useState } from "react";
import { jsx as _jsx } from "react/jsx-runtime";
export default function useAttachments(attachments, options) {
  var _useState = useState([]),
    _useState2 = _slicedToArray(_useState, 2),
    fileList = _useState2[0],
    setFileList = _useState2[1];
  var fileListRef = useRef([]);
  fileListRef.current = fileList;
  var getFileList = useCallback(function () {
    return fileListRef.current;
  }, []);
  var _ref = attachments || {},
    trigger = _ref.trigger,
    rest = _objectWithoutProperties(_ref, _excluded);
  var uidCounter = useRef(0);
  var handlePasteFile = useCallback(function (file) {
    if (!(rest !== null && rest !== void 0 && rest.customRequest)) return;
    var fileType = file.type || '';
    var fileName = file.name || '';
    if (rest.accept) {
      var matched = rest.accept.split(',').some(function (pattern) {
        var trimmed = pattern.trim();
        if (!trimmed) return false;
        if (trimmed.startsWith('.')) return fileName.toLowerCase().endsWith(trimmed.toLowerCase());
        if (trimmed === '*/*') return true;
        if (trimmed.includes('*')) {
          var _trimmed$split = trimmed.split('/'),
            _trimmed$split2 = _slicedToArray(_trimmed$split, 1),
            acceptMain = _trimmed$split2[0];
          var _fileType$split = fileType.split('/'),
            _fileType$split2 = _slicedToArray(_fileType$split, 1),
            fileMain = _fileType$split2[0];
          return acceptMain === fileMain;
        }
        return fileType === trimmed;
      });
      if (!matched) return;
    }
    if (rest.maxCount && fileListRef.current.length >= rest.maxCount) return;
    var getExtension = function getExtension() {
      var nameMatch = fileName.match(/\.([^.]+)$/);
      if (nameMatch) return nameMatch[1].toLowerCase();
      var typeMatch = fileType.match(/\/([^/+]+)/);
      return typeMatch ? typeMatch[1].toLowerCase() : 'bin';
    };
    var uid = "paste-".concat(Date.now(), "-").concat(uidCounter.current++);
    var uploadFile = {
      uid: uid,
      name: fileName || "pasted-".concat(Date.now(), ".").concat(getExtension()),
      size: file.size,
      type: fileType,
      status: 'uploading',
      percent: 0,
      originFileObj: file
    };
    setFileList(function (prev) {
      return [].concat(_toConsumableArray(prev), [uploadFile]);
    });
    if (fileType.startsWith('image/')) {
      var reader = new FileReader();
      reader.onload = function (e) {
        var _e$target;
        var dataUrl = (_e$target = e.target) === null || _e$target === void 0 ? void 0 : _e$target.result;
        if (typeof dataUrl === 'string') {
          setFileList(function (prev) {
            return prev.map(function (f) {
              return f.uid === uid ? _objectSpread(_objectSpread({}, f), {}, {
                thumbUrl: dataUrl
              }) : f;
            });
          });
        }
      };
      reader.readAsDataURL(file);
    }
    rest.customRequest({
      file: file,
      filename: 'file',
      action: '',
      method: 'POST',
      onSuccess: function onSuccess(response) {
        setFileList(function (prev) {
          return prev.map(function (f) {
            return f.uid === uid ? _objectSpread(_objectSpread({}, f), {}, {
              status: 'done',
              response: response,
              percent: 100
            }) : f;
          });
        });
      },
      onError: function onError(error) {
        setFileList(function (prev) {
          return prev.map(function (f) {
            return f.uid === uid ? _objectSpread(_objectSpread({}, f), {}, {
              status: 'error',
              error: error
            }) : f;
          });
        });
      },
      onProgress: function onProgress(event) {
        setFileList(function (prev) {
          return prev.map(function (f) {
            return f.uid === uid ? _objectSpread(_objectSpread({}, f), {}, {
              percent: event === null || event === void 0 ? void 0 : event.percent
            }) : f;
          });
        });
      }
    }, {
      defaultRequest: function defaultRequest() {
        return undefined;
      }
    });
  }, [rest === null || rest === void 0 ? void 0 : rest.customRequest, rest === null || rest === void 0 ? void 0 : rest.accept]);
  if (rest !== null && rest !== void 0 && rest.customRequest) {
    var uploadIconButton = /*#__PURE__*/_jsx(Upload, _objectSpread(_objectSpread({
      fileList: fileList,
      showUploadList: false,
      onChange: function onChange(info) {
        setFileList(info.fileList);
      }
    }, rest), {}, {
      disabled: options === null || options === void 0 ? void 0 : options.disabled,
      children: trigger ? /*#__PURE__*/React.createElement(trigger, {
        disabled: options === null || options === void 0 ? void 0 : options.disabled
      }) : /*#__PURE__*/_jsx(IconButton, {
        disabled: options === null || options === void 0 ? void 0 : options.disabled,
        icon: /*#__PURE__*/_jsx(SparkAttachmentLine, {}),
        bordered: false
      })
    }));
    var uploadFileListHeader = /*#__PURE__*/_jsx(Sender.Header, {
      closable: false,
      open: (fileList === null || fileList === void 0 ? void 0 : fileList.length) > 0,
      children: /*#__PURE__*/_jsx(Attachments, {
        items: fileList,
        onChange: function onChange(info) {
          return setFileList(info.fileList);
        }
      })
    });
    return {
      fileList: fileList,
      getFileList: getFileList,
      setFileList: setFileList,
      handlePasteFile: handlePasteFile,
      uploadIconButton: uploadIconButton,
      uploadFileListHeader: uploadFileListHeader
    };
  } else {
    return {
      enabled: false,
      handlePasteFile: undefined
    };
  }
}