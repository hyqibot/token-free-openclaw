function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }
function _iterableToArray(iter) { if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter); }
function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) return _arrayLikeToArray(arr); }
function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i]; return arr2; }
import { Bubble, useProviderContext } from "../../../..";
import { ChatAnywhereMessagesContext } from "../../Context/ChatAnywhereMessagesContext";
import { useContextSelector } from "use-context-selector";
import { ChatAnywhereSessionsContext } from "../../Context/ChatAnywhereSessionsContext";
import cls from 'classnames';
import Welcome from "../Welcome";
import { useChatAnywhereOptions } from "../../Context/ChatAnywhereOptionsContext";
import React from "react";
import { jsx as _jsx } from "react/jsx-runtime";
export default function MessageList(props) {
  var _bubbleListOptions$pa;
  var messages = useContextSelector(ChatAnywhereMessagesContext, function (v) {
    return v.messages;
  });
  var safeMessages = React.useMemo(function () {
    return _toConsumableArray(messages || []).reverse();
  }, [messages]);
  var prefixCls = useProviderContext().getPrefixCls('chat-anywhere-message-list');
  var currentSessionId = useContextSelector(ChatAnywhereSessionsContext, function (v) {
    return v.currentSessionId;
  });
  var bubbleListOptions = useChatAnywhereOptions(function (v) {
    var _v$theme;
    return (_v$theme = v.theme) === null || _v$theme === void 0 ? void 0 : _v$theme.bubbleList;
  });
  var listRef = React.useRef(null);
  var prevMessagesLengthRef = React.useRef(safeMessages.length);
  React.useEffect(function () {
    if (safeMessages.length > prevMessagesLengthRef.current) {
      var _listRef$current;
      (_listRef$current = listRef.current) === null || _listRef$current === void 0 || _listRef$current.scrollToBottom();
    }
    prevMessagesLengthRef.current = safeMessages.length;
  }, [safeMessages.length]);
  if (safeMessages.length === 0) return /*#__PURE__*/_jsx("div", {
    className: cls(prefixCls, "".concat(prefixCls, "-welcome")),
    children: /*#__PURE__*/_jsx(Welcome, {
      onSubmit: props.onSubmit
    })
  });
  return /*#__PURE__*/_jsx(Bubble.List, {
    ref: listRef,
    pagination: (_bubbleListOptions$pa = bubbleListOptions === null || bubbleListOptions === void 0 ? void 0 : bubbleListOptions.pagination) !== null && _bubbleListOptions$pa !== void 0 ? _bubbleListOptions$pa : true,
    order: "desc",
    classNames: {
      wrapper: prefixCls
    },
    items: safeMessages
  }, currentSessionId);
}